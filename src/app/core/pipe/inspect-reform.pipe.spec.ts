import { InspectReformPipe } from './inspect-reform.pipe';

describe('InspectReformPipe', () => {
  it('create an instance', () => {
    const pipe = new InspectReformPipe();
    expect(pipe).toBeTruthy();
  });
});
