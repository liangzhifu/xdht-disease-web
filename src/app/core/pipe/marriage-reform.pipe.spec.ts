import { MarriageReformPipe } from './marriage-reform.pipe';

describe('MarriageReformPipe', () => {
  it('create an instance', () => {
    const pipe = new MarriageReformPipe();
    expect(pipe).toBeTruthy();
  });
});
