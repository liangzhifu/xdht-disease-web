import { OfficeTypePipe } from './office-type.pipe';

describe('OfficeTypePipe', () => {
  it('create an instance', () => {
    const pipe = new OfficeTypePipe();
    expect(pipe).toBeTruthy();
  });
});
