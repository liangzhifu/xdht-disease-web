import { StatusReformPipe } from './status-reform.pipe';

describe('StatusReformPipe', () => {
  it('create an instance', () => {
    const pipe = new StatusReformPipe();
    expect(pipe).toBeTruthy();
  });
});
